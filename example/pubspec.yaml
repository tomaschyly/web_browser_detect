name: example
description: An example project for demonstration of usage of web_browser_detect.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0

  web_browser_detect:
    path: ../

flutter:
  uses-material-design: true
